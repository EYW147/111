# 发酵工程 Horizon 雷达

已完成的定制：

- 中文日报，48 小时信息窗口，每期最多 18 条。
- 重点关注代谢工程、合成生物学、工业微生物、菌株工程、生物反应器、过程控制、下游分离和生物制造。
- 信息源包括 Nature Biotechnology、Nature Chemical Biology、ISME Journal、bioRxiv、Google News、GDELT、GitHub 科研工具版本和 OSS Insight。
- 日报按“论文与科研进展 / 产业与生物制造 / 科研工具与数据分析”分组限额。
- AI 评分提示词已改成发酵工程科研视角，优先量化证据（产率、滴度、生产强度、规模和技术经济性），降低营销和泛健康内容权重。

## 还需要配置

### 必需：DeepSeek API Key

1. 登录 DeepSeek 开放平台并创建 API Key。
2. 本地测试时，把 Key 填入 `.env` 的 `DEEPSEEK_API_KEY=` 后面。
3. GitHub Actions 运行时，在你自己的仓库中进入 `Settings → Secrets and variables → Actions → New repository secret`，名称填 `DEEPSEEK_API_KEY`，值填 Key。

不要把真实 Key 写入 `data/config.github.json`，也不要提交 `.env`。

### 可选：GitHub Token

公开 GitHub 数据无需 Token 也能运行，但请求限额较低。本地可把 Token 放入 `.env` 的 `GITHUB_TOKEN=`；GitHub Actions 自带 `GITHUB_TOKEN`。

### 可选：消息推送

当前只生成 GitHub Pages 日报，不主动发消息。如需飞书、钉钉、Slack 或 Discord，可启用 `webhook`，再添加仓库 Secret `HORIZON_WEBHOOK_URL`。

## 本地运行

```powershell
.\run-local.ps1
```

日报输出在 `data/summaries/`，站点内容在 `docs/`。

启动脚本会自动启用 UTF-8，避免 Windows 中文终端因项目中的 emoji 输出而报错。

## GitHub 自动运行

将本目录推送到你自己的 GitHub 仓库后：

1. 添加 `DEEPSEEK_API_KEY` 仓库 Secret。
2. 打开 `Actions`，手动运行 `Daily Horizon Summary` 做首次测试。
3. 在 `Settings → Pages` 中选择 `Deploy from a branch`，分支选 `gh-pages`、目录选 `/ (root)`。

工作流当前每天北京时间约 07:00 触发（GitHub 定时任务可能延迟）。
